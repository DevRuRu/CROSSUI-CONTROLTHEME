name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:
jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
    steps:
      - uses: actions/checkout@v2
      - name: Install
        run: |
          sudo apt install -y libgc-dev
          git clone https://github.com/vlang/v /tmp/vlang
          pushd /tmp/vlang
        